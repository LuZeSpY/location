{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success mb-2"><i class="fa-regular fa-floppy-disk"></i> {{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}

<form method="post" action="{{ path('app_etat_lieux_delete', {'id': etat_lieux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ etat_lieux.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}EtatLieux{% endblock %}

{% block body %}
    <h1>EtatLieux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etat_lieux.id }}</td>
            </tr>
            <tr>
                <th>Date_etat_lieux</th>
                <td>{{ etat_lieux.dateEtatLieux ? etat_lieux.dateEtatLieux|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Remarque</th>
                <td>{{ etat_lieux.remarque }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_etat_lieux_index') }}">back to list</a>

    <a href="{{ path('app_etat_lieux_edit', {'id': etat_lieux.id}) }}">edit</a>

    {{ include('etat_lieux/_delete_form.html.twig') }}
{% endblock %}
